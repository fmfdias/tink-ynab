# Develop

## Initial start

```
$> bundle install
$> nvm use
$> docker-compose up -d
$> rails db:create
$> rails db:migrate
$> rails s
```

## Starting up

```
$> nvm use
$> docker-compose up -d
$> rails s
```

## Stopping

```
$>  docker-compose down
```